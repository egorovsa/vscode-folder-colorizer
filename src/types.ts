export interface PathColors {
  folderPath: string;
  color?: string;
  badge?: string;
  isForExtension?: boolean;
}

export interface PathsColors {
  folderPath: string[];
  color?: string;
  badge?: string;
  isForExtension?: boolean;
}

export interface IFind {
  color: string;
  badge: string;
}
